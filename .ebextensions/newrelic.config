container_commands: 
  set_permissions:
    command: |
      #!/bin/sh
      if grep -rnw '/etc/php.ini' -e 'newrelic.appname';
        then
      	  # code if found
      	  exit 0	
        else
      	  # code if not found
          echo "newrelic.appname = $ENV_NAME" >> /etc/php.ini
      fi