container_commands: 
  set_permissions:
    command: |
      #!/bin/sh
      if grep -Fxq "newrelic.appname" /etc/php.ini;
        then
      	  # code if found
      	  exit 0	
        else
      	  # code if not found
          echo "newrelic.appname = $ENV_NAME" >> /etc/php.ini
      fi